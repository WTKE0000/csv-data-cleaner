# CSV Data Cleaner (Python)

A small Python automation tool to clean messy CSV files and generate a summary report.

## Features
- Normalizes column names
- Trims whitespace
- Removes empty rows
- Removes duplicate rows
- Exports cleaned CSV
- Generates a summary report (missing values, duplicates removed, etc.)

## Install
```bash
pip install -r requirements.txt
